import React, { type ReactNode } from "react";

const FlexItemsStartG6 = ({ children }: { children: ReactNode }) => {
  return <div className={"flex items-start gap-6"}>{children}</div>;
};

export default FlexItemsStartG6;
